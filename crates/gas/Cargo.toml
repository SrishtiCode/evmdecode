[package]
name = "evmdecode-gas"
version = "0.1.0"
edition = "2021"

[lib]
name = "evmdecode_gas"
path = "src/lib.rs"

[dependencies]
evmdecode-core = { path = "../core" }
anyhow         = { workspace = true }
